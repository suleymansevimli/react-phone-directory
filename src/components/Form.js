import React from 'react';
import PropTypes from 'prop-types';

class Form extends React.Component{

    static propTypes = {
        
    };

    state = {

    };

    render(){
        return(
            <div>
                Form !
            </div>
        )
    }

}

export default Form;